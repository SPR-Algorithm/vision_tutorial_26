类定义：关键字、类名、访问修饰词、变量、方法、；结束
	public 确定了类成员的访问属性。在类对象作用域内，公共成员在类的外部是可访问的
	对象是根据类来创建 大约小
访问成员
	.length height set() get()
	类型 声明 详述（体积
	！私有的成员和受保护的成员不能使用直接成员访问运算符 (.) 来直接访问
成员函数 定义和原型写在类定义内部
构造/析构 生死用
拷贝构造 用同一类中之前创建的对象来初始化新创建
友元 访问同类下成员
内连 调用时拓展代码 再编辑
this 自指
指类 同指向结构的指针
静态成员  数据、函数成员
访问修饰符 public/private/protected
